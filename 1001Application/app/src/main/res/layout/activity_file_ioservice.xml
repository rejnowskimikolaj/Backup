<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_file_ioservice"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.rent.a1001application.FileIOActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:id="@+id/io_edit_text"
        android:hint="Type text " />

    <Button
        android:text="Save text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/io_edit_text"
        android:layout_alignStart="@+id/io_edit_text"
        android:layout_marginStart="36dp"
        android:layout_marginTop="41dp"
        android:id="@+id/button_save_text"
        android:onClick="saveText" />

    <Button
        android:text="Read Text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/button_save_text"
        android:layout_alignStart="@+id/button_save_text"
        android:layout_marginTop="19dp"
        android:id="@+id/button_read_text"
        android:onClick="readText" />
</RelativeLayout>
